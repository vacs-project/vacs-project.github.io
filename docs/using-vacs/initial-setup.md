---
sidebar_position: 1
---

# Initial setup

Before using **vacs** for the first time, several basic configuration steps must be completed. These ensure that the software is properly connected to your audio environment and the VATSIM network.

---

## Software Configuration

Before initial useage, you should configure the relevant settings of the software, such as:

- **Audio Devices**
- **Transmit Modes**
- **Other Key Bindings** (if used)

These settings are all explained in detail in the **Settings section** of the manual:

- [Audio Settings](/settings/audio)
- [Transmit Settings](/settings/transmit)

It is strongly recommended to review these settings before using vacs operationally.

---

## Check for Updates

Before starting a session, ensure that vacs is running **the most current version**.

If your version is outdated, an **update notification** will appear in the upper part of the interface.

<img
src="/img/using-vacs/update_not.png"
alt="vacs Settings Page"
style={{
    width: "80%",
    display: "block",
    margin: "1.5rem auto",
    borderRadius: "8px",
    boxShadow: "0 4px 16px rgba(0,0,0,0.08)"
  }}
/>

In this case, update the software before continuing.

Detailed instructions can be found here:
See [Updating VACS](/getting-started/updating) for detailed instructions.

---

## Login to VATSIM

After launching vacs, the **login screen** will appear.

<img
src="/img/using-vacs/login.png"
alt="vacs Settings Page"
style={{
    width: "80%",
    display: "block",
    margin: "1.5rem auto",
    borderRadius: "8px",
    boxShadow: "0 4px 16px rgba(0,0,0,0.08)"
  }}
/>

1. Press **Login via VATSIM**
2. Authenticate using your VATSIM account.
3. Once completed, your VATSIM account will be connected to vacs.

---

## Connect to vacs

After logging in, vacs will display the **connection screen**.

<img
src="/img/using-vacs/connect.png"
alt="vacs Settings Page"
style={{
    width: "80%",
    display: "block",
    margin: "1.5rem auto",
    borderRadius: "8px",
    boxShadow: "0 4px 16px rgba(0,0,0,0.08)"
  }}
/>

Before connecting, make sure that:

- You are **connected to VATSIM**
- Your **primary frequency is set**

Then press **Connect** to establish the vacs connection.

---

## Interface Layout

Once connected, vacs will load the interface layout configured by your vACC dataset.

Depending on the dataset configuration, this may be:

**Tabed Layout**
<img
src="/img/using-vacs/tabbed.png"
alt="vacs Settings Page"
style={{
    width: "80%",
    display: "block",
    margin: "1.5rem auto",
    borderRadius: "8px",
    boxShadow: "0 4px 16px rgba(0,0,0,0.08)"
  }}
/>

**GEO Layout**
<img
src="/img/using-vacs/geo.png"
alt="vacs Settings Page"
style={{
    width: "80%",
    display: "block",
    margin: "1.5rem auto",
    borderRadius: "8px",
    boxShadow: "0 4px 16px rgba(0,0,0,0.08)"
  }}
/>

These layouts provide **direct access keys** to stations that can be contacted for coordination.

If no dataset layout is defined, vacs will display a **generic layout** showing all currently connected positions on vacs.

---

## Ready for Use

Once:

- the software is **configured**
- the **latest version** is installed
- you are **logged into VATSIM**
- and **connected to vacs**

you are ready to begin using the system.

From this point onward, you can use the interface to **place and receive coordination calls with other controllers**.



